$(document).on('click', '#metamask-login', function(){
  var web3js;
  if(typeof web3 !== 'undefined'){
    web3js = new Web3(web3.currentProvider);
    console.log(web3js);

    alert("起動");
  } else {
    alert("Install metamask");
  }
});
